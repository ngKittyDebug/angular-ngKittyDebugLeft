/** A bomb explosion to render on the scene: a shockwave ring centred at (x, y) sized by `radius` (normalized). */
export interface Blast {
  id: string;
  x: number;
  y: number;
  radius: number;
}
